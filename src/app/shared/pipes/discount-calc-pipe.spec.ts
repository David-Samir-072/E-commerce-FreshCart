import { DiscountCalcPipe } from './discount-calc-pipe';

describe('DiscountCalcPipe', () => {
  it('create an instance', () => {
    const pipe = new DiscountCalcPipe();
    expect(pipe).toBeTruthy();
  });
});
